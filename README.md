# StudentPerformanceProject
A machine learning web application that predicts whether a student will pass or fail based on demographic and academic factors.  The app is built using Flask and deployed on Render, with a model trained in Python using scikit-learn and a full preprocessing pipeline.
